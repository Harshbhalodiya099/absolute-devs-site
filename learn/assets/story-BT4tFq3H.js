import{s as U,t as M,q as F,l as C,a as s,c as P,f as c,w as h,v as x,p as k,e as T,i as g,j as f,k as O,b as I,d as p,z as S}from"./scene-B3Z02THQ.js";import{s as B}from"./layout-9N9N7flX.js";import{d as H,q as N,v,t as u,l as W,a as E}from"./index-DL5uzQ2u.js";const d=E({glyph:"box",label:"Process",accent:"cyan",w:138,h:66,note:"A running program. It believes it owns the whole CPU and all of memory — the OS maintains that illusion for every process simultaneously."}),z=E({glyph:"gear",label:"Timer chip",accent:"rose",w:130,h:60,note:"A hardware clock that fires an interrupt at a fixed interval — typically every 1–10ms. This is the mechanism that yanks control from user code and hands it to the kernel. Without it, a greedy process could hold the CPU forever."}),j=U({id:"cooperative-crash",chapter:"The problem",question:"What happens when a program refuses to give up the CPU?",title:"One infinite loop, and the whole machine freezes.",takeaway:"Without a hardware timer interrupt, the CPU belongs to whoever is running — and there is no mechanism to take it back. Cooperative multitasking (classic Mac OS, Windows 3.x) trusted programs to yield voluntarily; one misbehaving program meant a frozen machine and a lost essay. The fix is a clock chip that fires an interrupt at a fixed interval, yanking control away whether the program likes it or not. That one piece of hardware is the entire difference between a frozen machine and a modern OS.",nextPrompt:"So the timer fires — then what actually happens?",prose:["Before we can appreciate what an operating system does, we need to feel what happens without one. Imagine a machine that runs programs one at a time, trusting each program to eventually say 'I'm done, your turn.' This is cooperative multitasking — and for a decade, it was how personal computers actually worked.","The rule is simple and fatal: whoever has the CPU keeps it until they choose to yield. Three well-behaved programs take polite turns. Then a fourth arrives — a buggy one with an infinite loop. It never yields. And because no mechanism exists to force it off the CPU, the other three programs, and you, wait forever.","This is not a hypothetical. Classic Mac OS and Windows 3.x worked exactly this way. One hung application froze the entire computer. The fix was not software cleverness — it was a hardware clock chip that fires an interrupt whether the running program wants it or not. Watch it break, then watch the fix."],setup:e=>{const{cpu:t}=e.cast({cpu:v.server({...B("center",{dy:-50}),label:"CPU",sub:"one core, one thing at a time",accent:"cyan",w:180,note:"The processor. It can only do one thing at a time. Right now it belongs to whoever is running — and there's no referee to say 'time's up.'"})}),{pA:a,pB:o,pC:r,greedy:n}=e.cast({pA:d({x:170,y:370,label:"Text editor",accent:"blue"}),pB:d({x:440,y:370,label:"Spreadsheet",accent:"green"}),pC:d({x:710,y:370,label:"Email",accent:"violet"}),greedy:d({...F(t,46),label:"Buggy app",sub:"while(true) {}",accent:"rose"})}),{taking:i,frozen:w,fix:b}=e.cast({taking:u({x:480,y:58,text:"cooperative: each program yields voluntarily",accent:"dim"}),frozen:u({x:480,y:58,text:"the machine is frozen — no mechanism to stop it",accent:"rose"}),fix:u({x:480,y:460,text:"the fix: a hardware timer that takes control back by force",accent:"green"})}),{timerChip:l}=e.cast({timerChip:z({x:810,y:170,label:"Timer chip",sub:"fires every ~10ms"})});e.step("Three well-behaved programs take turns on the CPU — each one yields voluntarily when it's done.",[C([t,a,o,r],.15),s(i),P(.4,c(a),c(o),c(r)),h(.3)]),e.step("A buggy program arrives. It has an infinite loop — and it never yields.",[x(i),s(n),k(n,1.8),T(g(n),c(t))]),e.step("The CPU belongs to whoever is running, and there is no mechanism to take it back — the machine is frozen.",[T(f(a),f(o),f(r)),O(t),s(w),h(.8)]),e.step("The fix is hardware, not software: a timer chip that fires an interrupt at a fixed interval — the bell that wakes the sleeping referee.",[x(w),s(l),c(l),s(b),k(l,2.4),h(.8)],{hold:1.2})}}),D=U({id:"the-illusion",chapter:"The trick",question:"What does the OS actually DO when the timer fires?",title:"The OS is not running right now. It wakes on a bell, swaps one program for another, and goes back to sleep.",takeaway:"The timer interrupt fires. User code halts mid-instruction. The CPU switches to kernel mode — a hardware flag, not a place — and runs the OS's trap handler. The handler saves the current process's registers (about 20 numbers), picks the next process, loads ITS registers, flips back to user mode, and returns. The OS is done. It was awake for microseconds. Both processes believe they were never interrupted. That is the entire trick, and it runs thousands of times per second.",nextPrompt:"Each process thinks it has the whole machine — but what about memory?",prose:["Here is the deepest misconception about operating systems: that the OS is 'always running in the background,' supervising like a manager watching over your shoulder. The truth is the opposite. While your code runs, the OS is not running at all. It is dormant code sitting in memory, doing nothing, waiting for one of three bells: a timer interrupt, a hardware event, or your program explicitly asking for help (a syscall).","When the timer fires, here is everything that happens: your code stops mid-instruction. The CPU flips a hardware mode bit from 'user' to 'kernel' — this is not a place, it's a privilege level. The CPU jumps to the OS's trap handler. The handler saves the current process's CPU state — about 20 register values, a few hundred bytes — and loads another process's saved state. Then it flips the mode bit back to 'user' and returns. Your process resumes, never knowing it was paused.","A context switch is not magic. It is copying twenty numbers. The mystique comes entirely from speed and invisibility — it happens thousands of times per second and no process ever sees it."],setup:e=>{const{cpu:t,kernel:a}=e.cast({cpu:v.server({x:480,y:130,label:"CPU",sub:"user mode",accent:"cyan",w:170,note:"One processor. The mode bit says who gets to touch hardware: user code or the kernel. While your code runs, the bit says 'user' and the OS is asleep."}),kernel:v.server({x:480,y:130,label:"CPU",sub:"kernel mode — the OS is awake",accent:"rose",w:170,note:"Same CPU, different privilege. Kernel mode lets the trap handler save registers, pick the next process, and load its state. It lasts microseconds."})}),{pA:o,pB:r,timerChip:n}=e.cast({pA:d({x:220,y:310,label:"Process A",sub:"running",accent:"cyan"}),pB:d({x:740,y:310,label:"Process B",sub:"ready (waiting)",accent:"violet"}),timerChip:z({x:810,y:130,label:"Timer",sub:"fires every 10ms"})}),{regA:i,regB:w,swap:b,asleep:l,awake:m}=e.cast({regA:u({...I(o,55),text:"registers: 20 numbers saved",accent:"cyan"}),regB:u({...I(r,55),text:"registers: 20 numbers loaded",accent:"violet"}),swap:u({x:480,y:450,text:"a context switch is copying ~20 numbers — that's all",accent:"green"}),asleep:W({x:480,y:60,text:"the OS is not running right now",size:13,color:"dim"}),awake:W({x:480,y:60,text:"the OS is awake — for microseconds",size:13,color:"rose"})}),y=e.connect(n,t,{bow:0,dashed:!0});e.step("Process A is running on the CPU in user mode — the OS is not running, it's dormant code in memory.",[C([t,o,r,n],.15),g(o),s(l),p(y)]),e.step("The timer fires — an interrupt. User code halts mid-instruction. The CPU flips to kernel mode.",[y.send({color:"rose",label:"interrupt!",dur:.8}),x(l),T(x(t),s(a)),s(m),O(o),h(.3)]),e.step("The kernel saves Process A's registers, picks Process B, and loads its registers — about 20 numbers, copied.",[S(o),s(i),c(o),h(.4),s(w),c(r),g(r),h(.3)]),e.step("Mode bit flips back to user. Process B runs, believing it was never interrupted. The OS goes back to sleep.",[x(m),T(x(a),s(t)),s(l),s(b),k(r,2.2),h(1)],{hold:1.2})}}),Y=U({id:"address-paradox",chapter:"The second illusion",question:"Two programs both use address 0x1000 — how is that possible?",title:"Every program starts at the same address. None of them share memory.",takeaway:"Every process gets its own virtual address space, starting from the same familiar addresses. When the CPU accesses memory, it doesn't use the virtual address directly — the hardware sends it through a page table, which translates it to a physical address. Two processes can both use address 0x1000, and the page table sends them to completely different physical locations. This is why one program can never accidentally read another's data, and why 'virtual memory' is not about swap files — it's about the illusion of private, contiguous memory.",nextPrompt:"So the OS manages time AND space — what choices does it actually make?",prose:["The CPU illusion was about time: every process believes it has the processor all to itself. The memory illusion is about space: every process believes it has a private, contiguous slab of memory, starting at address zero. Both processes can use address 0x1000 without conflict. How?","The trick is a hardware component called the MMU (memory management unit) and a data structure called the page table. When your code says 'read address 0x1000,' the CPU doesn't go to physical location 0x1000. It goes to the page table first — a per-process lookup table that translates virtual addresses to physical ones. Process A's 0x1000 maps to one physical location; Process B's 0x1000 maps to a completely different one.","This is the full meaning of 'virtual memory': not swap files, not 'extra memory on disk,' but the fundamental lie that each process has its own private memory space. The page table is the translator that keeps the lie consistent."],setup:e=>{const{pageTable:t}=e.cast({pageTable:v.server({...B("center",{dy:-30}),label:"Page table",sub:"virtual → physical",accent:"amber",w:170,note:"A per-process lookup table maintained by the OS and used by the hardware (MMU). It translates every virtual address to a physical one. Two processes with identical virtual addresses get routed to different physical memory."})}),{pA:a,pB:o}=e.cast({pA:d({x:170,y:200,label:"Process A",sub:"address 0x1000",accent:"cyan"}),pB:d({x:170,y:380,label:"Process B",sub:"address 0x1000",accent:"violet"})}),{physA:r,physB:n}=e.cast({physA:v.database({x:790,y:200,label:"Physical 0x8A00",accent:"cyan",w:160}),physB:v.database({x:790,y:380,label:"Physical 0x2F00",accent:"violet",w:160})}),{same:i,resolved:w,lesson:b}=e.cast({same:u({x:170,y:290,text:"same virtual address!",accent:"amber"}),resolved:u({x:790,y:290,text:"different physical memory",accent:"green"}),lesson:u({x:480,y:460,text:"virtual memory = private address space, not swap files",accent:"green"})}),l=e.connect(a,t,{bow:-20}),m=e.connect(o,t,{bow:20}),y=e.connect(t,r,{bow:-20}),A=e.connect(t,n,{bow:20});e.step("Two processes both use address 0x1000 — make a prediction: can that work?",[C([a,o],.2),s(i),k(i,1.8)]),e.step("Enter the page table — a per-process lookup that translates virtual addresses to physical ones.",[s(t),P(.15,p(l),p(m)),c(t)]),e.step("Process A's 0x1000 maps to physical 0x8A00; Process B's 0x1000 maps to physical 0x2F00 — completely different.",[l.send({color:"cyan",label:"0x1000",dur:.8}),s(r),p(y),y.send({color:"cyan",label:"→ 0x8A00",dur:.8}),h(.3),m.send({color:"violet",label:"0x1000",dur:.8}),s(n),p(A),A.send({color:"violet",label:"→ 0x2F00",dur:.8}),s(w)]),e.step("Virtual memory is not about swap files — it's the lie that every process has its own private, contiguous memory space.",[s(b),k(t,2.2),h(1)],{hold:1.2})}}),G=U({id:"scheduler",chapter:"The policy",question:"How often should the OS interrupt? And what happens if it doesn't?",title:"One slider between a responsive machine and a frozen one.",takeaway:"Preemptive scheduling is the difference between a machine that stays responsive and one that a single rogue process can freeze. With preemption on, the timer interrupt fires at a regular interval — the time quantum — and the OS rotates processes whether they like it or not. A short quantum means snappy UI but more context-switch overhead. A long quantum means efficiency but sluggish apps. And with preemption off, you're back to cooperative multitasking: one `while(true)` and the show is over. The entire trick rests on one clock chip.",nextPrompt:"Start the story again",prose:["The OS now has the power to take the CPU back by force — but how often should it? This is the scheduler's central trade-off, and it is interactive: you pick the regime and watch the consequences.","With preemption on and a normal time quantum, three processes take orderly turns. Each runs for its slice, the timer fires, the kernel saves and swaps, and the next process goes. Everyone makes progress. The machine feels responsive.","Turn preemption off and you're back in the cooperative world. Each process must yield voluntarily. Three polite programs work fine — but add a greedy one and the machine freezes. There is no timer to save you. A single toggle — hardware preemption on or off — is the entire difference between a modern OS and a 1990s crash."],params:{mode:M("Scheduling mode",[["preemptive","Preemptive — timer takes control"],["cooperative","Cooperative — programs yield voluntarily"]])},setup:(e,t)=>{const{cpu:a,timerChip:o}=e.cast({cpu:v.server({...B("top",{dy:10}),label:"CPU",sub:t.mode==="preemptive"?"timer fires every quantum":"no timer — cooperative",accent:t.mode==="preemptive"?"cyan":"amber",w:170,note:t.mode==="preemptive"?"Preemptive mode: the timer fires at a fixed interval and the OS swaps processes by force.":"Cooperative mode: no timer interrupt. Each process must voluntarily yield or everyone else waits forever."}),timerChip:z({x:820,y:135,label:"Timer",sub:t.mode==="preemptive"?"active":"disabled"})}),{p1:r,p2:n,p3:i}=e.cast({p1:d({x:230,y:370,label:"Editor",accent:"cyan"}),p2:d({x:480,y:370,label:"Browser",accent:"blue"}),p3:d({x:730,y:370,label:"Compiler",accent:"green"})}),w=e.connect(a,r,{bow:-20,dashed:!0}),b=e.connect(a,n,{bow:0,dashed:!0}),l=e.connect(a,i,{bow:20,dashed:!0}),m=e.connect(o,a,{bow:0,dashed:!0});if(t.mode==="preemptive"){const{verdict:q}=e.cast({verdict:u({x:480,y:460,text:"everyone gets fair turns — the timer is the referee",accent:"green"})});e.step("Three processes, one CPU, and a timer that fires at regular intervals — preemptive scheduling.",[C([a,r,n,i,o],.12),P(.1,p(w),p(b),p(l)),p(m)]),e.step("Process 1 runs for its time slice. The timer fires — the OS saves its state and picks the next one.",[g(r),c(a),h(.6),m.send({color:"rose",label:"tick!",dur:.5}),c(o),S(r)]),e.step("Process 2 runs, timer fires, swap. Process 3 runs, timer fires, swap. Everyone makes progress.",[g(n),c(a),h(.5),m.send({color:"rose",dur:.4}),S(n),g(i),c(a),h(.5),m.send({color:"rose",dur:.4}),S(i)]),e.step("Fair rotation, enforced by hardware — no process can hog the CPU because the timer takes it back every quantum.",[T(g(r),g(n),g(i)),s(q),k(o,2.2),h(1)],{hold:1.2});return}const{greedy:y}=e.cast({greedy:d({x:480,y:170,label:"Greedy app",sub:"while(true) {}",accent:"rose"})}),{doom:A}=e.cast({doom:u({x:480,y:460,text:"one toggle — preemption on/off — is the entire difference",accent:"rose"})});e.step("Cooperative mode: no timer, no forced swaps — each process must yield voluntarily.",[C([a,r,n,i],.12),P(.1,p(w),p(b),p(l)),s(o),f(o),p(m)]),e.step("The polite programs yield on time — cooperative works fine when everyone is well-behaved.",[P(.5,c(r),c(n),c(i)),h(.3)]),e.step("A greedy process arrives. It never yields. Without a timer, there is no mechanism to stop it.",[s(y),g(y),c(a),T(f(r),f(n),f(i)),O(a),h(.5)]),e.step("The machine is frozen — flip the toggle above to preemptive mode and watch the timer fix everything.",[s(A),O(y),k(y,2.4),h(1)],{hold:1.2})}}),V=H({...N,scenes:[j,D,Y,G],outro:["The whole trick, one more time: the OS is not a program that runs — it is dormant code that wakes when hardware traps into it, referees, and goes back to sleep. While your code runs, the OS is asleep. A timer chip fires thousands of times a second; each time the kernel wakes for microseconds, saves twenty numbers, loads twenty others, and vanishes. Both processes believe they were never interrupted. Add a page table that translates addresses per-process, and you have the second illusion: every program thinks it has private memory starting at zero.","Next time you glance at Activity Monitor or htop, you'll be reading this trick in real time: the CPU percentage columns are the kernel's bookkeeping of how it sliced one processor among many pretenders, and the memory column is virtual — every number is a lie that the page table makes consistent. The magic was never in complexity; it was in speed and a clock chip."],references:[{kind:"book",title:"Operating Systems: Three Easy Pieces",url:"https://pages.cs.wisc.edu/~remzi/OSTEP/",note:"The free textbook this story leans on — every chapter states the problem before revealing the mechanism."},{kind:"article",title:"Putting the You in CPU",url:"https://cpu.land",note:"An illustrated walk from 'you double-click a program' down to rings, syscalls, and ELF."},{kind:"video",title:"Ben Eater: Building an 8-bit computer",url:"https://eater.net/8bit",note:"Breadboard-level grounding for what a CPU, a clock, and an interrupt physically are."},{kind:"course",title:"MIT 6.1810 (xv6)",url:"https://pdos.csail.mit.edu/6.1810/",note:"A real Unix in ~6,000 lines of code — the build-it-yourself course if this story hooked you."}]});export{V as default};
