import{s as G}from"./index-DL5uzQ2u.js";import{E as e}from"./api-BBGmBOSo.js";import"./scene-B3Z02THQ.js";import"./layout-9N9N7flX.js";const E=e.definePreset({glyph:"database",label:"replica",accent:"dim",w:150,note:"A read-only copy of the database. It answers reads to take load off the primary, but every write must still go to the primary and stream down to it — so replicas scale reads, never writes."}),q=e.definePreset({glyph:"database",label:"shard",accent:"blue",w:150,note:"One partition of the data that owns a range of keys. A write for those keys goes here and nowhere else — which is finally how writes scale past a single machine, at the cost of coordinating who owns what."}),x="Real people hitting your app. They don't know or care how many machines you run — only whether the page loads, and how fast.",y="An app server that keeps no memory of who you are between requests. Because any copy can answer any request, the load balancer is free to send traffic anywhere — this is the property that makes 'more boxes' possible.",k={x:110,y:262},R=e.scene({id:"one-box",chapter:"Base camp",question:"You shipped it and people came. What actually breaks first when a crowd shows up?",title:"One box serves everyone — right up until it can't.",takeaway:"Almost everything starts as a single server talking to a single database, and for a long time that is exactly right — simplicity is a feature. What breaks first is rarely the code: it's capacity. One machine has a fixed ceiling of CPU, memory, and connections, and when the crowd exceeds it the box doesn't fail politely — it slows to a crawl and starts refusing requests. Everything that follows is a different answer to the same question: how do you serve more people than one machine can?",nextPrompt:"So do you buy a bigger box, or more boxes?",setup:a=>{const{users:i,app:t,db:o}=a.cast({users:e.v.users({...k,sub:"a handful, then a crowd",note:x}),app:e.v.server({x:470,y:262,label:"app server",sub:"1 box · handles it all",note:"Your application, running on one machine. Every request — page, login, checkout — is served here, and every request competes for the same finite CPU and memory."}),db:e.v.database({x:790,y:262,sub:"the one source of truth",note:"Where all the durable state lives: users, orders, sessions. One server can answer a lot of queries — until it can't, and then everything above it waits."})}),s=a.connect(i,t,{bow:34,dashed:!0}),l=a.connect(t,o,{bow:-20,dashed:!0});a.step("At the start it's the whole architecture: users hit one app server, which reads and writes one database.",[e.enter([i,t,o],.2),e.draw(s),e.draw(l),e.all(s.send({label:"GET /",dur:1}),e.seq(e.wait(.4),l.send({color:"amber",label:"query",dur:.9})))]),a.step("For a real while, this is genuinely enough — and there is nothing wrong with it being simple.",[e.all(e.pulse(t,2.4),e.pulse(o,2.4)),e.seq(s.send({label:"GET /",dur:.9}),s.send({label:"GET /",dur:.9}))]);const{load:r,err:c}=a.cast({load:e.token({...e.above(t,60),text:"traffic ↑↑↑",accent:"amber"}),err:e.token({...e.below(t,62),text:"503 · out of capacity",accent:"rose"})});a.step("Then the crowd arrives faster than one machine can answer, and the box runs out of CPU and connections.",[e.appear(r),e.all(e.shake(t),e.pulse(t,2.6)),e.seq(s.send({color:"rose",label:"GET /",dur:1.1,keepAlive:!0}),e.appear(c))]),a.step("One machine has one ceiling; past it, requests don't slow gracefully, they get refused — so the whole game is serving more than one box can.",[e.all(e.pulse(t,2.2),e.pulse(c,2.2)),e.wait(1)],{hold:1,view:"all"})}}),D=e.scene({id:"the-fork",chapter:"The fork in the trail",question:"More capacity — sure. But do you grow the one machine, or add machines beside it?",title:"A bigger box has a ceiling. More boxes need your app to forget you.",takeaway:"There are exactly two directions. Scaling vertically means a bigger box: dead simple, but it hits a hard ceiling and stays a single point of failure — one crash is still a full outage. Scaling horizontally means more boxes behind a load balancer: it scales far past any single machine and adds redundancy, but it only works if the app is stateless. If any server can answer any request, traffic sprays freely; if a server 'remembers' your session, you're pinned to it and horizontal scaling breaks. Statelessness is the price of admission — move session out of the app and into shared state.",nextPrompt:"Now every read still lands on the same database. Can we make reads cheap?",setup:a=>{const{users:i,box:t,db:o}=a.cast({users:e.v.users({...k,note:x}),box:e.v.server({x:470,y:262,label:"app server",sub:"one machine",note:"The single box from base camp. The first instinct is to make this one thing bigger; the more durable instinct is to make more of it."}),db:e.v.database({x:800,y:262,sub:"shared state",note:"Still the one database. Both strategies keep it — for now."})}),s=a.connect(i,t,{bow:34,dashed:!0}),l=a.connect(t,o,{bow:-20,dashed:!0});a.step("Start from the one box again, and take the first, obvious option: make it bigger.",[e.enter([i,t,o],.18),e.draw(s),e.draw(l),s.send({label:"GET /",dur:.9})]);const{grow:r,ceil:c}=a.cast({grow:e.token({...e.above(t,60),text:"vertical · +CPU +RAM",accent:"cyan"}),ceil:e.token({...e.below(t,62),text:"one crash = full outage",accent:"rose"})});a.step("Vertical scaling works and buys real headroom — but it's still one machine, so one crash is still a full outage.",[e.appear(r),e.pulse(t,2.4),e.appear(c)]);const{lb:b,app1:p,app2:f}=a.cast({lb:e.v.loadBalancer({x:330,y:262,sub:"sprays traffic",note:"Sits in front of the fleet, spreads each request across the healthy app servers (round-robin or least-connections), and quietly drops any box that stops answering health checks."}),app1:e.v.server({x:570,y:160,label:"app · A",sub:"stateless",note:y}),app2:e.v.server({x:570,y:364,label:"app · B",sub:"stateless",note:y})}),m=a.connect(i,b,{bow:0,dashed:!0}),T=a.fanout(b,[p,f],{dashed:!0,bowSpread:52}),A=a.connect(p,o,{bow:22,dashed:!0}),w=a.connect(f,o,{bow:-22,dashed:!0});a.step("The other direction: put a load balancer in front and add identical boxes beside the first.",[e.all(e.fadeTo(r,0,.3),e.fadeTo(c,0,.3),e.fadeTo(s,0,.3),e.fadeTo(l,0,.3),e.vanish(t)),e.appear(b),e.draw(m),e.enter([p,f],.18)]),a.step("It only works because the apps are stateless — any box can answer any request, so the balancer sends traffic anywhere.",[T.draw({gap:.14,dur:.5}),e.all(e.draw(A),e.draw(w)),T.send({color:"cyan",label:"GET /",gap:.16,dur:.9})]);const{rule:d}=a.cast({rule:e.token({...e.below(o,66),text:"session lives here, not in the app",accent:"green"})});a.step("The catch: session state must move out of the app into shared storage — statelessness is the price of scaling out.",[e.appear(d),e.all(A.send({color:"amber",label:"session",dur:.9}),w.send({color:"amber",label:"session",dur:.9})),e.pulse(o,2.2),e.wait(.9)],{hold:1,view:"all"})}}),j=e.scene({id:"the-cache",chapter:"A cache near the summit",question:"You can add app servers all day — but they all still hammer the one database. What gives?",title:"Most traffic is reads, and the same reads over and over.",takeaway:"Adding app servers just moves the pressure downstream: they all read from the same database, and reads are the overwhelming majority of most traffic — the same popular rows, fetched again and again. A cache in front of the database is the single highest-leverage move in scaling. A read checks the cache first; on a hit it never touches the database at all, and only a miss falls through. Suddenly the database serves a fraction of the reads it used to, and the whole system breathes. Caching doesn't remove the database bottleneck — it starves it of the easy work so it can survive the hard work.",nextPrompt:"Reads are cheap now. But what about the writes?",setup:a=>{const{users:i,lb:t,app1:o,app2:s,cache:l,db:r}=a.cast({users:e.v.users({...k,note:x}),lb:e.v.loadBalancer({x:300,y:262,sub:"sprays traffic",note:"The same front door: spreads requests across the stateless fleet."}),app1:e.v.server({x:500,y:160,label:"app · A",sub:"stateless",note:y}),app2:e.v.server({x:500,y:364,label:"app · B",sub:"stateless",note:y}),cache:e.v.cache({x:700,y:150,sub:"hot reads, in memory",note:"An in-memory store of the most-requested data. Answers in microseconds, absorbs the repeated reads, and only lets a genuine miss reach the database behind it."}),db:e.v.database({x:700,y:366,sub:"source of truth",note:"The durable store. With a cache in front, it now sees only cache misses and writes — a small slice of the old load."})}),c=a.connect(i,t,{bow:0,dashed:!0}),b=a.fanout(t,[o,s],{dashed:!0,bowSpread:52}),p=a.connect(o,l,{bow:16,dashed:!0}),f=a.connect(s,r,{bow:-16,dashed:!0}),m=a.connect(l,r,{bow:0,dashed:!0,color:"dim"});a.step("Here's the fleet from before — every one of these boxes still reads from the single database.",[e.enter([i,t,o,s,r],.14),e.draw(c),b.draw({gap:.14,dur:.5}),e.draw(f)]),a.step("So put a cache in front of it: a read checks the cache first, and most of the time the answer is already there.",[e.appear(l),e.draw(p),e.draw(m),p.send({color:"cyan",label:"read?",dur:.8})]);const{hit:T}=a.cast({hit:e.token({...e.below(l,60),text:"cache hit · DB untouched",accent:"green"})});a.step("On a hit, the request is answered from memory and never touches the database at all.",[p.reply({color:"green",label:"hit",dur:.8}),e.appear(T),e.all(e.pulse(l,2.2),e.dim(r))]);const{miss:A}=a.cast({miss:e.token({...e.below(r,60),text:"only misses fall through",accent:"amber"})});a.step("Only a miss falls through to the database — so it serves a fraction of the reads, and the highest-traffic path is now the cheapest.",[e.undim(r),m.send({color:"amber",label:"miss",dur:.9}),e.appear(A),e.pulse(r,2),e.wait(.9)],{hold:1,view:"all"})}}),M=e.scene({id:"the-wall",chapter:"The wall below the peak",question:"Cached reads are cheap. But every write still lands on one database. How do writes scale?",title:"Reads copy easily. Writes are where scaling gets hard.",takeaway:"The database is the real summit wall, and reads and writes hit it differently. Reads scale by copying: add read replicas — read-only followers of the primary — and spread reads across them. But writes can't just be copied, because every copy would have to agree, so writes stay pinned to the primary. To scale writes you must split the data itself into shards, each owning a range of keys, so different writes go to different machines. That buys write throughput — and reintroduces a hard question: when a write's ownership is in doubt, which shard is authoritative? Agreeing on that under failure is exactly the majority/consensus problem from 'Jury duty for servers': a group of machines picking one answer even when some are down.",nextPrompt:"Now take the dial yourself — and choose bigger-box or more-boxes.",setup:a=>{const{app:i,primary:t}=a.cast({app:e.v.server({x:165,y:262,label:"app tier",sub:"×N stateless",note:y}),primary:e.v.database({x:400,y:262,label:"primary",sub:"takes every write",note:"The one database that accepts writes. Reads can be copied away from it, but writes cannot — every write has to be ordered somewhere, and that somewhere is here."})}),o=a.connect(i,t,{bow:0,dashed:!0});a.step("Reads are handled — but every write in the whole system still funnels into one primary database.",[e.enter([i,t],.18),e.draw(o),o.send({color:"amber",label:"write",dur:.9}),e.pulse(t,2)]);const{rep1:s,rep2:l}=a.cast({rep1:E({x:700,y:150,sub:"read-only copy"}),rep2:E({x:700,y:262,sub:"read-only copy"})}),r=a.fanout(t,[s,l],{dashed:!0,bowSpread:44,color:"dim"});a.step("Reads scale the easy way — by copying: read replicas follow the primary and answer reads off to the side.",[e.enter([s,l],.16),r.draw({gap:.14,dur:.5}),r.send({color:"cyan",label:"stream changes",gap:.16,dur:.9})]);const{stuck:c}=a.cast({stuck:e.token({...e.below(t,66),text:"writes still land here only",accent:"rose"})});a.step("But you can't copy a write — every copy would have to agree — so writes stay stuck on the single primary.",[e.appear(c),e.all(e.dim(s),e.dim(l)),e.all(e.shake(t),e.pulse(t,2.4))]);const{shardA:b,shardB:p}=a.cast({shardA:q({x:615,y:300,label:"shard · A",sub:"keys A–M"}),shardB:q({x:800,y:300,label:"shard · B",sub:"keys N–Z"})}),f=a.connect(i,b,{bow:30,dashed:!0,color:"blue"}),m=a.connect(i,p,{bow:-30,dashed:!0,color:"blue"});a.step("To scale writes you split the data itself into shards — each owns a range of keys, so different writes go to different machines.",[e.all(e.fadeTo(c,0,.3),e.fadeTo(o,0,.3),e.fadeTo(r.wires[0],0,.3),e.fadeTo(r.wires[1],0,.3),e.vanish(s),e.vanish(l),e.vanish(t)),e.enter([b,p],.16),e.draw(f),f.send({color:"blue",label:"write A–M",dur:1})]),a.step("But now: which shard truly owns a contested write? Agreeing under failure is the same majority problem from 'Jury duty for servers'.",[e.draw(m),m.send({color:"blue",label:"write N–Z",dur:1}),e.all(e.pulse(b,2.2),e.pulse(p,2.2)),e.wait(1)],{hold:1,view:"all"})}}),N={solo:"2 vCPU · 8 GB",balanced:"16 vCPU · 64 GB",cached:"64 vCPU · 256 GB",distributed:"maxed · no bigger box exists"},O=e.scene({id:"take-the-dial",chapter:"Take the dial",question:"You choose: as the crowd grows, do you buy a bigger box or add more boxes?",title:"The same load, two very different systems.",takeaway:"This is the whole story in one control. Flip to 'bigger box' and the system never changes shape — it's always one machine, just with more CPU and RAM, which is beautifully simple and a single point of failure that eventually hits a ceiling no purchase can clear. Flip to 'more boxes' and each step up in load rebuilds the system: a lone server, then a balancer over stateless apps, then a cache absorbing reads, then replicas and shards splitting the data tier. Vertical is a straight line to a wall; horizontal is a ladder with a harder rung at every level — and the hardest rungs are always at the database. Take the crowd to millions on 'bigger box' and watch it redline; switch to 'more boxes' and watch the cast grow to meet it.",nextPrompt:"Start the climb again",params:{scale:e.choice("Crowd size",[["solo","A few users"],["balanced","Thousands"],["cached","Tens of thousands"],["distributed","Millions"]]),grow:e.toggle("Strategy",[["horizontal","More boxes"],["vertical","Bigger box"]])},setup:(a,i)=>{if(i.grow==="vertical"){const w=i.scale==="distributed",{users:d,box:n,db:u}=a.cast({users:e.v.users({...k,note:x}),box:e.v.server({x:480,y:262,w:180,label:"app server",sub:N[i.scale],note:"The bigger-box strategy: one machine, bought larger each time the crowd grows. Simple to reason about — and the only thing that can fail is also the only thing there is."}),db:e.v.database({x:800,y:262,sub:"one source of truth",note:"Still a single database, growing with the box."})}),h=a.connect(d,n,{bow:34,dashed:!0}),v=a.connect(n,u,{bow:-20,dashed:!0});a.step("The bigger-box strategy never changes shape: no matter the crowd, it's one machine — you just buy a larger one.",[e.enter([d,n,u],.18),e.draw(h),e.draw(v),h.send({label:"GET /",dur:.9})]);const{spof:B}=a.cast({spof:e.token({...e.below(n,70),text:"single point of failure",accent:"rose"})});if(!w){a.step("It comfortably handles this load — but there is exactly one of it, so a single crash is still a total outage.",[e.appear(B),e.all(e.pulse(n,2.4),e.pulse(u,2)),e.wait(.9)],{hold:1,view:"all"});return}const{ceil:S,err:g}=a.cast({ceil:e.token({...e.above(n,62),text:"ceiling hit · nothing bigger to buy",accent:"amber"}),err:e.token({...e.below(n,128),text:"503 · out of capacity",accent:"rose"})});a.step("But at millions, you run out of box: no machine is big enough, and it redlines with nowhere left to grow.",[e.appear(B),e.appear(S),e.all(e.shake(n),e.pulse(n,2.6)),e.seq(h.send({color:"rose",label:"GET /",dur:1.1,keepAlive:!0}),e.appear(g))]),a.step("Vertical scaling is a straight line to a wall — to go further you must stop growing the box and start adding boxes.",[e.all(e.pulse(n,2.2),e.pulse(g,2.2)),e.wait(1)],{hold:1,view:"all"});return}if(i.scale==="solo"){const{users:w,app:d,db:n}=a.cast({users:e.v.users({...k,sub:"a few",note:x}),app:e.v.server({x:470,y:262,label:"app server",sub:"1 box",note:y}),db:e.v.database({x:790,y:262,sub:"source of truth",note:"One database, easily keeping up at this size."})}),u=a.connect(w,d,{bow:34,dashed:!0}),h=a.connect(d,n,{bow:-20,dashed:!0});a.step("A few users: one box is the right amount of machine — there is nothing to spread yet.",[e.enter([w,d,n],.2),e.draw(u),e.draw(h),e.all(u.send({label:"GET /",dur:.9}),e.seq(e.wait(.4),h.send({color:"amber",label:"query",dur:.8})))]),a.step("Adding machines now would be pure overhead — you scale out when one box fills up, not before.",[e.all(e.pulse(d,2.2),e.pulse(n,2.2)),e.wait(.9)],{hold:1,view:"all"});return}if(i.scale==="balanced"){const{users:w,lb:d,app1:n,app2:u,db:h}=a.cast({users:e.v.users({...k,sub:"thousands",note:x}),lb:e.v.loadBalancer({x:330,y:262,sub:"sprays traffic",note:"Spreads each request across the healthy app servers and drops any that stop answering."}),app1:e.v.server({x:560,y:160,label:"app · A",sub:"stateless",note:y}),app2:e.v.server({x:560,y:364,label:"app · B",sub:"stateless",note:y}),db:e.v.database({x:800,y:262,sub:"shared state",note:"Sessions live here, not in the apps — which is what lets any box answer any request."})}),v=a.connect(w,d,{bow:0,dashed:!0}),B=a.fanout(d,[n,u],{dashed:!0,bowSpread:52}),S=a.connect(n,h,{bow:22,dashed:!0}),g=a.connect(u,h,{bow:-22,dashed:!0});a.step("Thousands: a load balancer fronts two stateless app servers, and traffic sprays across both.",[e.enter([w,d,n,u,h],.14),e.draw(v),B.draw({gap:.14,dur:.5}),e.all(e.draw(S),e.draw(g)),B.send({color:"cyan",label:"GET /",gap:.16,dur:.9})]),a.step("Because no app remembers you, session state lives in the shared database — the enabler for every box you add.",[e.all(S.send({color:"amber",label:"session",dur:.9}),g.send({color:"amber",label:"session",dur:.9})),e.pulse(h,2.2),e.wait(.9)],{hold:1,view:"all"});return}if(i.scale==="cached"){const{users:w,lb:d,app1:n,app2:u,cache:h,db:v}=a.cast({users:e.v.users({...k,sub:"tens of thousands",note:x}),lb:e.v.loadBalancer({x:280,y:262,sub:"sprays traffic",note:"The same front door over a bigger fleet."}),app1:e.v.server({x:470,y:160,label:"app · A",sub:"stateless",note:y}),app2:e.v.server({x:470,y:364,label:"app · B",sub:"stateless",note:y}),cache:e.v.cache({x:680,y:150,sub:"hot reads",note:"Absorbs the repeated reads in memory; only a miss reaches the database."}),db:e.v.database({x:680,y:366,sub:"misses + writes",note:"Now sees only cache misses and writes — a fraction of the old read load."})}),B=a.connect(w,d,{bow:0,dashed:!0}),S=a.fanout(d,[n,u],{dashed:!0,bowSpread:52}),g=a.connect(n,h,{bow:16,dashed:!0}),P=a.connect(u,v,{bow:-16,dashed:!0}),C=a.connect(h,v,{bow:0,dashed:!0,color:"dim"});a.step("Tens of thousands: the same fleet, now with a cache in front of the database catching the repeated reads.",[e.enter([w,d,n,u,h,v],.12),e.draw(B),S.draw({gap:.14,dur:.45}),e.all(e.draw(g),e.draw(P),e.draw(C)),g.send({color:"cyan",label:"read?",dur:.8})]),a.step("A hit answers from memory and never touches the database; only a miss falls through — reads got cheap.",[g.reply({color:"green",label:"hit",dur:.8}),C.send({color:"amber",label:"miss",dur:.9}),e.all(e.pulse(h,2.2),e.pulse(v,2)),e.wait(.9)],{hold:1,view:"all"});return}const{users:t,lb:o,app:s,cache:l,shardA:r,shardB:c,rep:b}=a.cast({users:e.v.users({...k,sub:"millions",note:x}),lb:e.v.loadBalancer({x:270,y:262,sub:"sprays traffic",note:"Front door for the whole stateless fleet."}),app:e.v.server({x:440,y:262,label:"app tier",sub:"×N stateless",note:y}),cache:e.v.cache({x:640,y:120,sub:"hot reads",note:"Still the first stop for reads, absorbing the repeated majority."}),shardA:q({x:610,y:290,label:"shard · A",sub:"keys A–M"}),shardB:q({x:800,y:290,label:"shard · B",sub:"keys N–Z"}),rep:E({x:705,y:412,sub:"per-shard read copy"})}),p=a.connect(t,o,{bow:0,dashed:!0}),f=a.connect(o,s,{bow:0,dashed:!0}),m=a.connect(s,l,{bow:10,dashed:!0}),T=a.fanout(s,[r,c],{dashed:!0,bowSpread:40,color:"blue"}),A=a.connect(r,b,{bow:20,dashed:!0,color:"dim"});a.step("Millions: the app tier is just ×N stateless boxes behind the balancer — the whole fight has moved to the data tier.",[e.enter([t,o,s],.16),e.draw(p),e.draw(f),p.send({color:"cyan",label:"GET /",dur:.8})]),a.step("Reads hit the cache; writes split across shards, each owning a key range, so writes finally scale past one machine.",[e.appear(l),e.enter([r,c],.16),e.draw(m),T.draw({gap:.14,dur:.5}),e.all(m.send({color:"cyan",label:"read",dur:.8}),T.send({color:"blue",label:"write by key",gap:.16,dur:.9}))]),a.step("Each shard keeps its own read replica to spread its reads — reads copy easily, the same trick one level down.",[e.appear(b),e.draw(A),A.send({color:"cyan",label:"stream",dur:.9}),e.pulse(b,2)]),a.step("And agreeing which shard owns a contested write, even as machines fail, is the majority problem from 'Jury duty for servers'.",[e.all(e.pulse(r,2.2),e.pulse(c,2.2)),e.wait(1)],{hold:1,view:"all"})}}),L=e.story({...G,scenes:[R,D,j,M,O],outro:["Strip away the diagrams and scaling is one honest admission repeated at every level: no single machine is enough, so you spread the work — and spreading only works when the pieces stop depending on any one place. App servers spread trivially once they forget who you are. Reads spread by copying into caches and replicas. Writes are the stubborn ones, because a write is a decision about the truth, and truth can't simply be duplicated — it has to be owned, which is why the final move is sharding the data itself.","That's also the through-line to the rest of these explainers. The moment you shard, you're back to a room full of machines that must agree on one answer while some of them are down — the majority rule from 'Jury duty for servers.' Scaling doesn't escape the hard problems of distributed systems; it climbs high enough that it finally has to face them. The bigger box was never the summit. The summit is a system with no single point that has to be big at all."],references:[{kind:"book",title:"Designing Data-Intensive Applications — Kleppmann",url:"https://dataintensive.net/",note:"Chapters 5 (replication) and 6 (partitioning/sharding) are the definitive treatment of why the database is the wall."},{kind:"docs",title:"AWS Well-Architected Framework — Reliability & Performance",url:"https://docs.aws.amazon.com/wellarchitected/latest/framework/welcome.html",note:"Horizontal scaling, statelessness, and load balancing framed as first principles for building at scale."},{kind:"book",title:"Google SRE Book — Load Balancing",url:"https://sre.google/sre-book/load-balancing-frontend/",note:"How real traffic gets spread across a fleet at the frontend and inside the datacenter."},{kind:"article",title:"The AKF Scale Cube",url:"https://akfpartners.com/growth-blog/scale-cube",note:"The x/y/z axes of scaling — cloning, splitting by function, and splitting by data — the framing behind sharding."}]});export{L as default};
