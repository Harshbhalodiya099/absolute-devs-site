import{s as E,n as l,a as N,b as C,y as _,g as c,c as t,d as w,h as p,j as O,e as r,w as i,p as m,m as Y,i as S,z as $,r as M,f as j,k as q,u as D,x as ee,l as te,t as oe,q as ae}from"./scene-Bqfy8Rb5.js";import{d as ne,b as se}from"./index-BGQIqiO5.js";const re=E({id:"one-server",chapter:"The problem",question:"What happens when the thing running your app just… stops?",title:"One container. One server. Nobody watching.",takeaway:"Everything crashes eventually — that part is normal and unavoidable. The real problem is that recovery depended on a human noticing and acting. Kubernetes exists to delete that dependency: to make a crash a non-event instead of an outage.",nextPrompt:"What if something was always watching?",prose:["Here is the smallest possible way to run software for other people: one container, holding your app, sitting on one server. A request comes in, the container answers it, the request goes home happy. For a surprisingly long time, this is genuinely enough — it's how most things start, and there's nothing wrong with it.","The trouble isn't that it's simple. The trouble is that it's alone. A container is just a process, and processes die: a memory leak fills the heap, a bad deploy ships a crash-on-startup bug, the kernel reaps something to save itself. None of these are exotic. Given enough nights, one of them will happen — and it will happen at the worst possible hour.","The figure below plays out that night. Watch what happens after the crash: nothing. No alarm inside the system, no reflex, no recovery. The next request simply finds an empty room, and it keeps finding one until a human wakes up. That gap — between 'it broke' and 'someone noticed' — is the entire reason the rest of this story exists."],setup:e=>{const{users:o,pod:g}=e.cast({users:l({x:150,y:262,glyph:"user",label:"Users",sub:"just want the page",accent:"blue",note:"Real people hitting your app. They don't know or care how many servers you run — only whether the page loads."}),pod:l({x:680,y:262,glyph:"box",label:"your-app",sub:"1 container · 1 server",accent:"green",note:"A container is just a running process with its dependencies packed in. Isolated and portable — but still mortal. When it dies, nothing here brings it back."})}),{clock:s,err:n,downSays:h}=e.cast({clock:_({x:680,y:110,text:"03:00",size:26,color:"amber"}),err:N({...C(g,66),text:"503 · no healthy backend",accent:"rose"}),downSays:ce(o,["“Is the site down?”"])}),u=e.connect(o,g,{bow:40,dashed:!0}),y=e.packet(u,{color:"blue",label:"GET /"}),b=e.packet(u,{color:"blue",label:"GET /"}),v=e.packet(u,{color:"rose",label:"GET /"});e.step("For months this is enough: a request arrives, the one container answers, everyone goes home happy.",[c(.25,t(o),t(g)),w(u,.6),p(m(g,3.2),O(r(y,1.1),i(.2),r(b,1.1)))]),e.step("Then, one night, it crashes — a memory leak, a bad deploy, a kernel hiccup. Which one hardly matters.",[t(s),Y(g),i(.7),p($(g),S(g,.12,.6))]),e.step("Nothing is watching. The next request finds an empty room — and it stays that way until a human wakes up.",[r(v,1.3,{keepAlive:!0}),p(t(n),O(i(.3),t(h))),i(1.4)],{hold:1.2})}}),ie=E({id:"control-loop",chapter:"The one big idea",question:"What if the fix wasn't a person, but a loop?",title:"Stop starting containers. Start declaring wishes.",takeaway:"This is the whole trick, repeated a thousand ways: you declare a desired state, a controller observes the actual state, and it acts to close the gap — continuously, forever. Self-healing isn't a feature bolted on top of Kubernetes; it's what the machine is made of.",nextPrompt:"But who actually runs the loop?",prose:["The fix isn't a smarter human or a faster pager. It's a change in what you tell the computer. Instead of imperatively starting and babysitting containers — 'run this, now restart it, now run another' — you hand over a single declarative wish: I want three copies of this app running, at all times. Then you walk away.","Something called a controller takes that wish and does exactly one thing, over and over: it compares. Desired state, three. Actual state, three. Match — do nothing. It is almost boringly simple, and that boredom is the point. There's no clever recovery logic to get wrong, because there's no special 'recovery' path at all.","So watch what happens when a pod dies in the figure below. To the controller it isn't an emergency — it's just the two numbers disagreeing for a moment. Three wanted, two running. It schedules one more, the numbers match again, and the loop goes back to being boring. Kill another and it'll do the same thing. That's the whole idea; everything else is detail."],setup:e=>{const{controller:o,field:g,p1:s,p2:n,p3:h,p2b:u,matchTok:y,gapTok:b,healTok:v}=e.cast({controller:l({x:185,y:262,glyph:"gear",label:"Controller",sub:"desired: 3 replicas",accent:"violet",note:"A controller is a small program running one endless loop: read the desired state, look at the actual state, and take the smallest action that moves reality toward the wish."}),field:M({x:655,y:262,w:300,h:400,title:"actually running",accent:"dim",note:"The observed world: whatever pods are alive right now. The controller never trusts this to stay correct — it re-checks it constantly."}),p1:d(655,132),p2:d(655,262),p3:d(655,392),p2b:d(655,262),matchTok:N({x:185,y:392,text:"desired 3 = running 3 ✓",accent:"green"}),gapTok:N({x:185,y:392,text:"running 2 — gap!",accent:"rose"}),healTok:N({x:185,y:392,text:"reconciled → 3 ✓",accent:"green"})}),I=e.connect(o,s,{bow:60,dashed:!0}),x=e.connect(o,n,{bow:0,dashed:!0}),f=e.connect(o,h,{bow:-60,dashed:!0}),k=e.packet(e.route(o,u,{bow:0}),{color:"violet",label:"start a replacement"});e.step("You declare a wish, not a procedure: three copies of this app, always. The controller writes it down.",[t(o),t(g),c(.2,t(s),t(n),t(h)),c(.15,w(I,.5),w(x,.5),w(f,.5))]),e.step("Its entire job is to compare the wish to reality. Three wanted, three running — a match. So it does nothing, happily.",[p(m(o,2.4),c(.15,j(s),j(n),j(h))),O(i(.4),t(y)),i(.6)]),e.step("Now kill one — a node reboots, a pod runs out of memory. Reality drifts: three wanted, two running.",[S(y,0,.3),Y(n),i(.5),p(S(n,0,.5),S(x,.15,.5)),t(b),i(.6)]),e.step("The gap is all it needs. It schedules a fresh pod into the hole, and the count is three again — no human, no pager.",[m(o,3),S(b,0,.3),r(k,1.3),t(u),p(q(u),t(v)),i(1.2)],{hold:1.2})}}),he=E({id:"anatomy",chapter:"Under the hood",question:"So who runs the loop — and where does it all live?",title:"One door in. A shared memory. Machines that never stop watching.",takeaway:"A cluster has two halves. The control plane decides — the API server is the only door in, etcd is the memory, and the scheduler and controllers are the deciders. The worker nodes do — each runs a kubelet that turns assignments into real containers. You only ever talk to the door; everything else is machines watching etcd and reconciling.",nextPrompt:"So what does a machine dying actually cost?",prose:["That loop has to run somewhere, and it helps to see the cluster split cleanly in two. On one side is the control plane — the brain, where decisions are made. On the other are the worker nodes — the muscle, where your containers actually run. Almost everything interesting happens as information flows from brain to muscle.","The brain has three named parts worth knowing. The API server is the only door in: every command, from you or from a controller, goes through it and nowhere else. etcd is the memory — a consistent key-value store that holds the one true record of what should exist. And the scheduler is a matchmaker, deciding which node each new pod should live on. A rule of thumb: if it isn't written in etcd, it didn't happen.","Follow a single `kubectl apply` through the figure and the whole system stops feeling mysterious. Your command hits the door, gets written to memory, wakes the matchmaker, and finally a kubelet on some node turns the plan into a running container. No component ever gives another an order directly — they each just watch etcd and react. That indirection is exactly what makes the whole thing survivable."],setup:e=>{const{kubectl:o,plane:g,api:s,etcd:n,sched:h,nodes:u,node1:y,node2:b,pod:v,placed:I}=e.cast({kubectl:l({x:118,y:118,glyph:"laptop",label:"kubectl apply",sub:"you, asking",accent:"blue",note:"The command line. It doesn't run anything itself — it just sends your desired state to the one endpoint that accepts changes."}),plane:M({x:470,y:268,w:540,h:430,title:"Control plane — the brain",accent:"dim"}),api:l({x:355,y:148,glyph:"server",label:"API server",sub:"the only door in",accent:"blue",note:"Every read and write goes through here. It validates the request and persists it — but it does not act on it. It just records intent."}),etcd:l({x:355,y:392,glyph:"database",label:"etcd",sub:"the cluster's memory",accent:"amber",note:"A distributed, consistent key-value store. It holds the single source of truth for the entire cluster. Lose etcd and you lose the cluster's mind."}),sched:l({x:610,y:148,glyph:"gear",label:"Scheduler",sub:"the matchmaker",accent:"violet",note:"Watches for pods with no assigned node, scores every node by free CPU, memory, affinity and constraints, and binds each pod to the best fit."}),nodes:M({x:838,y:268,w:190,h:430,title:"Worker nodes — the muscle",accent:"dim"}),node1:l({x:838,y:208,glyph:"server",label:"Node 1",sub:"kubelet",accent:"cyan",note:"A worker machine. Its kubelet watches the API server for pods assigned to it, then tells the container runtime to actually start them."}),node2:l({x:838,y:392,glyph:"server",label:"Node 2",sub:"kubelet",accent:"cyan",note:"Another worker. Identical role. The scheduler chose Node 1 for this pod, so Node 2 sits this one out."}),pod:d(838,92),placed:N({...C(L(838,92),4),text:"container started ✓",accent:"green"})}),x=e.connect(o,s,{bow:20,dashed:!0}),f=e.connect(s,n,{bow:-40,dashed:!0}),k=e.connect(s,y,{bow:55,dashed:!0}),G=e.packet(x,{color:"blue",label:"run this pod"}),P=e.packet(f,{color:"amber",label:"write desired state"}),z=e.packet(e.route(h,y,{bow:30}),{color:"violet",label:"score"}),W=e.packet(e.route(h,b,{bow:-30}),{color:"violet",label:"score"}),B=e.packet(e.route(h,s,{bow:60}),{color:"violet",label:"bind → Node 1"}),K=e.packet(e.route(s,y,{bow:55}),{color:"green",label:"kubelet: start it"});e.step("Every change enters through exactly one door — the API server. `kubectl apply` hands it your desired state.",[t(g),p(t(o),t(s)),D([o,s,n],{margin:90}),w(x,.6),r(G,1.3),m(s,1.8)]),e.step("The API server doesn't act on it. It writes it to etcd — the cluster's single source of truth. If it isn't in etcd, it never happened.",[t(n),w(f,.5),r(P,1.2),j(n),i(.6)]),e.step("The new pod has no home yet. The scheduler watches for homeless pods, scores each node, and picks the best fit.",[ee(),p(t(h),t(u),c(.2,t(y),t(b))),m(h,3),O(i(.3),p(r(z,.9),r(W,.9))),r(B,1.1),m(s,1.4)]),e.step("On the winning node, the kubelet sees the assignment and actually starts the container. The wish is finally a running thing.",[D([s,y,v],{margin:90}),w(k,.5),r(K,1.2),t(v),p(q(v),t(I),m(y,2)),i(1.2)],{hold:1.2})}}),le=E({id:"self-healing",chapter:"Why it feels like magic",question:"A whole machine just died. Why didn't your users notice?",title:"Pods are cattle. The address in front of them never moves.",takeaway:"A Service is a stable front door with a fixed address; the pods behind it are cattle, not pets — replaced freely, on any node, without the caller ever knowing. Put the reconciliation loop behind that address and losing an entire machine becomes a shrug: the survivors keep serving while replacements spin up elsewhere. Toggle the cluster above and compare.",nextPrompt:"Start the story again",prose:["Two ideas from earlier now snap together into the thing that feels like magic. The reconciliation loop keeps the right number of pods alive. But pods are disposable — they get replaced, land on different machines, and receive new IP addresses every time. If your users had to know a pod's address, healing would break them constantly.","So they never do. In front of the pods sits a Service: a single, stable address that never changes, even as the pods behind it come and go. Every request goes to the Service, and the Service forwards it to whatever healthy pods happen to exist at that instant. Callers talk to the front door; they have no idea — and no need to know — which particular pod answered.","The figure below is interactive. It starts healthy: one Service spreading traffic across four pods on two nodes. Then flip the toggle and kill an entire node. Watch the Service quietly drop the dead pods, keep serving from the survivors, and pick up the rescheduled replacements — all behind the same address, with your users none the wiser."],params:{state:oe("Cluster",[["healthy","Healthy"],["failure","Node fails"]])},setup:(e,o)=>{const g=o.state==="failure",{users:s,svc:n}=e.cast({users:l({x:112,y:262,glyph:"user",label:"Users",accent:"blue",visible:!0,note:"They only ever know one address — the Service's. What runs behind it is invisible to them, by design."}),svc:l({x:342,y:262,glyph:"balancer",label:"Service",sub:"one stable address",accent:"cyan",visible:!0,note:"A fixed virtual IP that never changes. It tracks the current set of healthy pods and load-balances across them, so callers never chase a moving target."})}),h=e.connect(s,n,{bow:0,dashed:!0});if(!g){const{a1:a,a2:A,b1:F,b2:H}=e.cast({node1:l({x:632,y:148,glyph:"server",label:"Node 1",accent:"cyan",visible:!0}),node2:l({x:632,y:392,glyph:"server",label:"Node 2",accent:"cyan",visible:!0}),a1:d(842,96),a2:d(842,208),b1:d(842,332),b2:d(842,444)}),U=[a,A,F,H],Q=U.map((T,Z)=>e.connect(n,T,{bow:40-Z*26,dashed:!0})),V=e.packet(h,{color:"blue",label:"GET /"}),X=U.map(T=>e.packet(e.route(n,T,{bow:0}),{color:"cyan"}));e.step("Behind one Service address sit four pods across two nodes. The Service is the only thing your users ever talk to.",[c(.12,t(a),t(A),t(F),t(H)),w(h,.4),c(.1,...Q.map(T=>w(T,.4)))]),e.step("Every request hits the Service, which spreads it across whatever healthy pods exist right now.",[r(V,.8),c(.18,...X.map(T=>r(T,.8))),p(...U.map(T=>m(T,1.8))),i(1)],{hold:1});return}const{node1:u,node2:y,a1:b,a2:v,dead1:I,dead2:x,r1:f,r2:k,downTok:G}=e.cast({node1:l({x:632,y:150,glyph:"server",label:"Node 1",accent:"cyan",visible:!0}),node2:l({x:632,y:396,glyph:"server",label:"Node 2",sub:"dead",accent:"cyan",visible:!0}),a1:d(842,84),a2:d(842,190),dead1:d(842,336),dead2:d(842,448),r1:d(842,296),r2:d(842,408),downTok:N({...C(L(632,396),8),text:"Node 2 lost",accent:"rose"})}),P=[b,v],z=P.map((a,A)=>e.connect(n,a,{bow:30-A*20,dashed:!0})),W=e.packet(h,{color:"blue",label:"GET /"}),B=P.map(a=>e.packet(e.route(n,a,{bow:0}),{color:"cyan"})),K=[f,k].map((a,A)=>e.connect(u,a,{bow:-20+A*12,dashed:!0})),R=[f,k].map(a=>e.packet(e.route(u,a,{bow:0}),{color:"violet",label:"reschedule"})),J=[f,k].map(a=>e.packet(e.route(n,a,{bow:0}),{color:"cyan"}));e.step("Same cluster, now under stress. Then Node 2 dies outright — power, kernel, hardware — and both of its pods vanish with it.",[c(.1,t(b),t(v),t(I),t(x)),w(h,.4),c(.1,...z.map(a=>w(a,.4))),i(.3),Y(y),i(.5),p(te(y),S(I,0,.5),S(x,0,.5),t(G))]),e.step("The Service simply stops routing to the dead pods and keeps serving from the survivors. Your users see nothing.",[r(W,.7),c(.2,...B.map(a=>r(a,.8))),p(m(b,1.8),m(v,1.8)),i(.8)]),e.step("Meanwhile the controller notices two pods missing and reschedules them — here, onto Node 1. New pods, new IPs.",[m(u,3),c(.15,...K.map(a=>w(a,.4))),c(.2,...R.map(a=>r(a,.9))),c(.15,t(f),t(k)),p(q(f),q(k))]),e.step("Same address, different pods, zero downtime. Pods are disposable; the Service in front of them is forever.",[r(W,.7),c(.18,...J.map(a=>r(a,.8))),p(m(f,1.8),m(k,1.8),q(n)),i(1.2)],{hold:1.2})}}),ue=ne({...se,scenes:[re,ie,he,le],outro:["Strip away the vocabulary and Kubernetes is one small idea taken seriously: never tell the computer what to do — tell it what you want, and let a loop close the gap forever. A pod dies, the numbers disagree, the loop fixes it. A node dies, more numbers disagree, the loop fixes that too. There is no separate 'disaster recovery' mode because, from the loop's point of view, a disaster and an ordinary Tuesday look exactly the same.","That's also why the parts are shaped the way they are. One door in (the API server) so every change is recorded and ordered. One memory (etcd) so there's a single truth to reconcile against. Stable Services in front of disposable pods so healing never breaks the caller. None of it is magic — it's delegation and comparison, the same two ideas that quietly run most robust systems, arranged so that the 3 A.M. crash from the first scene resolves itself before anyone's phone even buzzes."]});function d(e,o){return l({x:e,y:o,w:132,glyph:"box",label:"pod",sub:"your-app",accent:"green",note:"A pod wraps one (or a few tightly-coupled) containers. It's the smallest thing Kubernetes schedules — and the smallest thing it will happily throw away and recreate."})}function ce(e,o){return ae({...C(e,84),w:210,lines:o,accent:"blue"})}function L(e,o){return{x:e,y:o+42}}export{ue as default};
