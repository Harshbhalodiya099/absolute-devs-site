import{s as O,t as U,n,a as A,b as V,c as t,p as l,d as s,e as c,f as q,g as x,h,i as H,j as K,k as y,w as f,l as $,m as E,r as Q,o as X}from"./scene-Bqfy8Rb5.js";import{d as Z,m as _}from"./index-BGQIqiO5.js";const ee=O({id:"bare-vm",chapter:"Climbing the ladder",question:"It runs on your laptop. How does the rest of the world reach it?",title:"Rent a computer that never sleeps.",takeaway:"A virtual machine is a computer in someone's datacenter, rented by the hour. You SSH in, install the runtime by hand, copy your code up, and open a port — and now the world can reach it. Dead simple to picture, but you own everything: OS patches, the drift between your laptop and the server, and the 3 A.M. crash that stays down until you wake up.",nextPrompt:"How do I make it run the same everywhere?",prose:["Your code works. It works on your laptop, which is the one machine on Earth where it is guaranteed to work, because your laptop is exactly the computer you built it on. The moment you want anyone else to use it, you hit the oldest problem in shipping software: your laptop goes to sleep, gets closed, rides the train home. A thing the public depends on cannot do that. It needs a computer that is always on.","The most direct answer is to rent one. A virtual machine is a sliver of a real server in a datacenter, handed to you as if it were a whole computer — you get an IP address and a root login. So you SSH in and set it up the way you'd set up any fresh machine: install the language runtime, copy your code across, install the dependencies, start the process, open a firewall port. Point a domain at the IP and, genuinely, you have shipped.","It's the clearest mental model there is, which is exactly why it's worth starting here. It's also the one where every future problem is born. You built this server by hand, which means no two servers you build are quite alike. The OS needs patching and that's your job now. And when the process dies at 3 A.M. — out of memory, unhandled exception, a full disk — nothing brings it back. The lights just stay off until you notice."],setup:e=>{const{laptop:d,vm:a,users:o}=e.cast({laptop:n({x:150,y:260,glyph:"laptop",label:"Your laptop",sub:"code that works",accent:"blue",note:"The one machine where your app is guaranteed to run — because it's the machine you built it on."}),vm:n({x:510,y:260,w:176,glyph:"server",label:"Virtual machine",sub:"rented by the hour",accent:"cyan",note:"A sliver of a real server, handed to you as a whole computer: an IP address and a root login. You own everything above the bare metal."}),users:n({x:820,y:260,glyph:"user",label:"Users",sub:"the whole world",accent:"violet",note:"They reach your app by its public IP or domain name — as long as the process behind it is alive."})}),r=e.connect(d,a,{bow:60,dashed:!0}),i=e.connect(a,o,{bow:-40}),m=e.packet(r,{color:"blue",label:"ssh + scp"}),b=e.packet(e.route(o,a,{bow:-40}),{color:"violet",label:"GET /"}),u=e.packet(e.route(a,o,{bow:40}),{color:"green",label:"200 OK"}),{install:p,copy:g,run:w,crash:v}=e.cast({install:A({x:510,y:150,text:"install runtime",accent:"cyan"}),copy:A({x:510,y:118,text:"copy code",accent:"cyan"}),run:A({x:510,y:372,text:"open port :443",accent:"cyan"}),crash:A({...V(a,96),text:"crash → still down",accent:"rose"})});e.step("It all starts on the one computer where your code is certain to run: the machine you wrote it on.",[t(d),l(d,1.6)]),e.step("So you rent a virtual machine, SSH in, and set it up by hand — runtime, code, an open port.",[t(a),s(r,.7),c(m,1.3),q(a),x(.18,t(g),t(p),t(w)),h(H(g,.45),H(p,.45),H(w,.45))]),e.step("Point a domain at its address and you've genuinely shipped — the world hits it and your app answers.",[t(o),s(i,.6),c(b,1.1),h(l(a,1.8),K(f(.4),c(u,1.1),y(o)))]),e.step("But you own all of it — patches, configuration drift, and the crash at 3 A.M. that stays down until you wake up.",[t(v),h(E(a),$(a),l(v,2.2)),f(1.4)],{hold:1})}}),te=O({id:"docker-image",chapter:"Climbing the ladder",question:"How do I make it run the same everywhere?",title:"Ship the whole kitchen, not just the recipe.",takeaway:"A Docker image packs your code together with its exact libraries, runtime, and a slice of the operating system into one immutable artifact. Build it once, push it to a registry, and anyone can pull that exact image and run it — a server, a laptop, a CI runner — and get identical behaviour. The unit you deploy is no longer instructions to reproduce an environment; it's the environment itself.",nextPrompt:"One container is easy — what about a hundred that heal themselves?",prose:["The rot in the hand-built server is drift. Your laptop has Node 20 and a certain OpenSSL; the server drifted to Node 18 and a different one; a colleague is on something else again. Each is a machine assembled by hand at a different moment, so 'works on my machine' isn't a joke — it's a precise statement that the environment, not the code, is what differs. Fix that and you fix a whole category of 3 A.M. surprises.","Docker's move is to stop shipping instructions and start shipping the result. `docker build` reads a short recipe and produces an image: your code, its dependencies, the language runtime, and a thin slice of the operating system, frozen together into one immutable, content-addressed artifact. It isn't a description of an environment. It is the environment, packed into a file.","You push that image to a registry — Docker Hub, ECR, GHCR — once. From then on, running your app anywhere is a `docker pull` and a `docker run`. The bits that execute on the production server are the exact same bits that ran on your laptop and in CI, byte for byte. You still need somewhere to run the container, and that's the next rung — but the thing you deploy is now perfectly reproducible."],setup:e=>{const{laptop:d,image:a,registry:o,server:r,teammate:i}=e.cast({laptop:n({x:150,y:260,glyph:"laptop",label:"Build machine",sub:"docker build",accent:"blue",note:"Reads a Dockerfile — a short recipe — and produces an image. Run once, not on every deploy."}),image:n({x:440,y:150,w:190,glyph:"box",label:"Image",sub:"code + libs + OS",accent:"amber",note:"One immutable, content-addressed artifact. Not a description of an environment — the environment itself, frozen."}),registry:n({x:440,y:390,w:190,glyph:"cloud",label:"Registry",sub:"Docker Hub · ECR",accent:"violet",note:"A shared store for images. Push once; pull anywhere. The image's digest guarantees you get the exact same bits."}),server:n({x:790,y:150,glyph:"server",label:"Prod server",sub:"docker run",accent:"cyan",note:"Any host with a container runtime. It doesn't need your language installed — the image already carries it."}),teammate:n({x:790,y:390,glyph:"laptop",label:"CI runner",sub:"same image",accent:"green",note:"The bits that run here are byte-for-byte the bits that ran in prod. 'Works on my machine' stops being possible."})}),m=e.connect(d,a,{bow:-20,dashed:!0}),b=e.connect(a,o,{bow:0,dashed:!0}),u=e.connect(o,r,{bow:-30,dashed:!0}),p=e.connect(o,i,{bow:30,dashed:!0}),g=e.packet(m,{color:"amber",label:"build"}),w=e.packet(b,{color:"violet",label:"push"}),v=e.packet(u,{color:"cyan",label:"pull"}),S=e.packet(p,{color:"green",label:"pull"}),{same:T}=e.cast({same:A({x:790,y:270,text:"identical bits",accent:"green"})});e.step("The rot in a hand-built server is drift: two machines assembled by hand are never quite the same.",[t(d),l(d,1.6)]),e.step("`docker build` freezes your code, its libraries, and a slice of the OS into one immutable image.",[t(a),s(m,.6),c(g,1.1),h(q(a),l(a,1.8))]),e.step("You push that image to a registry once — a single, content-addressed artifact anyone can fetch.",[t(o),s(b,.6),c(w,1),q(o)]),e.step("Then any host pulls the exact same image and runs it — server, laptop, CI — with no more 'works on my machine'.",[x(.2,t(r),t(i)),x(.2,s(u,.5),s(p,.5)),h(c(v,1.1),K(f(.3),c(S,1.1))),h(y(r),y(i),t(T)),f(1.2)],{hold:1})}}),ae=O({id:"kubernetes",chapter:"Climbing the ladder",question:"One container is easy — what about a hundred that heal themselves?",title:"Hand the cluster a wish, not a to-do list.",takeaway:`Kubernetes runs your image across a fleet of machines. You don't launch containers by hand — you declare desired state ("five replicas of this image") and a controller makes reality match: it schedules pods across nodes, load-balances traffic, and when a pod or a whole machine dies, it notices the gap and fills it. You stop tending servers and start describing what you want.`,nextPrompt:"But not everything is a server — where does a website, or a database, live?",prose:["One container is a solved problem: `docker run` and walk away. Real systems aren't one container. They're many copies of your app for capacity, spread across several machines so one failure isn't total, wired to a load balancer, and — the hard part — expected to recover on their own at 3 A.M. Doing that by hand means writing exactly the babysitting scripts that Kubernetes already is.","The shift Kubernetes asks of you is to stop giving orders and start declaring intent. You don't say 'start a container here, another there.' You hand it a wish: I want five replicas of this image, reachable on this port. That desired state is stored, and a control loop compares it against what's actually running, forever — the same idea underneath everything Kubernetes does.","From that one idea, the magic falls out. Kubernetes schedules the five pods across nodes and load-balances traffic to them. A pod crashes and now four are running against five wanted — the loop sees the gap and starts a replacement. A whole node dies, taking its pods with it; the same comparison reschedules them elsewhere. There is no special 'disaster' mode: a catastrophe and an ordinary Tuesday look identical to a loop whose only job is to close the gap."],setup:e=>{const{image:d,deploy:a,users:o}=e.cast({image:n({x:150,y:120,w:150,glyph:"box",label:"Your image",accent:"amber",note:"The same immutable artifact from the last rung. Kubernetes runs copies of it — it doesn't care what's inside."}),deploy:n({x:150,y:360,w:168,glyph:"gear",label:"Deployment",sub:"desired: 5 replicas",accent:"cyan",note:"Your declared intent, stored in the cluster. A control loop compares it against reality without stopping."}),users:n({x:150,y:240,glyph:"user",label:"Users",accent:"violet",note:"Traffic enters through a load balancer that spreads requests across whichever pods are currently healthy."})}),r=e.add(Q({x:620,y:265,w:440,h:370,title:"Cluster · nodes",accent:"ink"})),i=(Y,C)=>n({x:Y,y:C,w:96,h:52,glyph:"box",label:"pod",accent:"green",note:"One running copy of your image."}),{p1:m,p2:b,p3:u,p4:p,p5:g,p6:w}=e.cast({p1:i(510,175),p2:i(630,175),p3:i(750,175),p4:i(560,350),p5:i(700,350),p6:i(750,350)}),v=e.connect(d,a,{bow:0,dashed:!0}),S=e.connect(a,r,{bow:30,dashed:!0}),T=e.connect(o,r,{bow:-10}),k=e.packet(S,{color:"cyan",label:"desired: 5"}),I=e.packet(T,{color:"violet",label:"traffic"});e.step("Real systems aren't one container — they're many copies, on many machines, expected to recover on their own.",[t(d),t(a),s(v,.6),l(a,1.6)]),e.step("You never launch them by hand. You hand Kubernetes a wish: five replicas of this image.",[s(S,.6),c(k,1.1),t(r),x(.14,t(m),t(b),t(u),t(p),t(g)),h(y(m),y(b),y(u),y(p),y(g))]),e.step("It schedules them across nodes and load-balances traffic in — you've stopped tending servers.",[t(o),s(T,.6),c(I,1.2),l(r,1.8)]),e.step("A pod dies. The loop sees five wanted, four running — and fills the gap before anyone notices.",[h(E(u),H(u,.06,.5)),f(.4),l(a,2),f(.3),t(w),y(w),f(1.2)],{hold:1})}}),oe=O({id:"two-homes",chapter:"The fork in the road",question:"Not everything is a server — where does a website, or a database, actually live?",title:"Two things you're deploying, two completely different homes.",takeaway:"A single-page app is stateless: pure files with no server logic, so it lives on a CDN — copied to edge locations worldwide, served from the nearest one in milliseconds, scaling to millions for almost nothing. A database is the exact opposite: all state, one source of truth, pinned to a persistent disk that must survive every reboot. You can't casually clone it or scale it to zero. Frontend, backend, and data each want a different kind of home — match the host to the thing.",nextPrompt:"So which rung do you climb to?",prose:["The whole ladder so far assumed you're deploying a running program — a process that answers requests. But two of the most common things you deploy aren't that shape at all, and forcing them onto a plain server is where a lot of cost and pain come from. Flip the control below between the two and watch how differently they want to live.","A single-page app, once built, is just files: HTML, JavaScript, CSS. There is no server-side logic to execute — the logic runs in the user's browser. So the ideal home isn't a server at all; it's a CDN. You copy the built bundle to edge locations scattered across the planet, and every visitor is served from the one nearest them, in milliseconds. It scales to millions of users essentially for free, because serving a static file is the cheapest thing the internet does.","A database is the mirror image. It is nothing but state — the one authoritative copy of your data — and state cannot be casually duplicated the way a stateless app can. Writes must funnel to a single primary; every byte has to survive a reboot on a persistent disk; read replicas and backups exist precisely because this box is irreplaceable. It can't scale to zero and you can't run ten identical copies. It's the opposite discipline from the SPA, which is exactly why they belong in different homes."],params:{serving:U("Serving",[["spa","A single-page app"],["db","A database"]])},setup:(e,d)=>{if(d.serving==="spa"){const{bundle:k}=e.cast({bundle:n({x:150,y:260,w:168,glyph:"doc",label:"Static bundle",sub:"HTML · JS · CSS",accent:"amber",note:"The built output of your SPA. No server logic — all the code runs later, in the user's browser."})}),I=(L,G,J)=>n({x:L,y:G,w:132,h:66,glyph:"cloud",label:J,sub:"edge cache",accent:"cyan",note:"A CDN edge location. Holds a copy of the bundle so nearby users never touch your origin."}),{e1:Y,e2:C,e3:P,e4:R}=e.cast({e1:I(520,120,"Frankfurt"),e2:I(800,190,"Mumbai"),e3:I(520,410,"São Paulo"),e4:I(800,380,"Tokyo")}),{visitor:D,cost:z}=e.cast({visitor:n({x:300,y:430,glyph:"user",label:"A visitor",accent:"violet",note:"Served from the closest edge — not from your build machine, which may be a continent away."}),cost:A({x:520,y:265,text:"scales to millions · ~$0",accent:"green"})}),j=e.connect(k,Y,{bow:-20,dashed:!0}),B=e.connect(k,C,{bow:-10,dashed:!0}),M=e.connect(k,P,{bow:20,dashed:!0}),N=e.connect(k,R,{bow:10,dashed:!0}),F=e.connect(D,P,{bow:20}),W=e.packet(e.route(P,D,{bow:-20}),{color:"green",label:"12 ms"});e.step("A single-page app, once built, is just files — there's no server-side logic to run.",[t(k),l(k,1.6)]),e.step("So you copy that bundle to a CDN: the same files cached at edge locations around the planet.",[x(.14,t(Y),t(C),t(P),t(R)),x(.1,s(j,.5),s(B,.5),s(M,.5),s(N,.5)),h(c(e.packet(j,{color:"cyan"}),1),c(e.packet(B,{color:"cyan"}),1),c(e.packet(M,{color:"cyan"}),1),c(e.packet(N,{color:"cyan"}),1))]),e.step("Every visitor is served from the nearest edge in milliseconds — it scales to millions for almost nothing.",[t(D),s(F,.5),c(W,.9),h(y(D),l(P,1.8),t(z)),f(1.2)],{hold:1});return}const{app:a,primary:o,disk:r}=e.cast({app:n({x:150,y:260,glyph:"server",label:"Your app",sub:"reads + writes",accent:"cyan",note:"Every write in your whole system funnels down to one place."}),primary:n({x:500,y:200,w:170,glyph:"database",label:"Primary",sub:"source of truth",accent:"green",note:"The single authoritative copy. Writes go here and only here — there is exactly one."}),disk:n({x:500,y:400,w:170,glyph:"doc",label:"Persistent disk",sub:"survives reboots",accent:"amber",note:"State lives on durable storage. Lose this and you lose the data — the whole point of the box."})}),{r1:i,r2:m,backup:b,warn:u}=e.cast({r1:n({x:820,y:130,w:150,glyph:"database",label:"Read replica",accent:"cyan",note:"A near-copy that serves reads to spread load. It cannot accept writes — those still go to the primary."}),r2:n({x:820,y:290,w:150,glyph:"database",label:"Read replica",accent:"cyan",note:"More replicas share read traffic, but none of them is the source of truth."}),backup:n({x:820,y:430,w:150,glyph:"box",label:"Backups",accent:"violet",note:"Point-in-time copies. When the irreplaceable box fails, this is the only way back."}),warn:A({...X(a,8),text:"can't scale to zero",accent:"rose"})}),p=e.connect(a,o,{bow:-20}),g=e.connect(o,r,{bow:0}),w=e.connect(o,i,{bow:-20,dashed:!0}),v=e.connect(o,m,{bow:0,dashed:!0}),S=e.connect(o,b,{bow:25,dashed:!0}),T=e.packet(p,{color:"cyan",label:"write"});e.step("A database is the opposite of a static app: it's nothing but state, with one source of truth.",[t(a),t(o),s(p,.6),c(T,1),h(q(o),l(o,1.8))]),e.step("Writes funnel to a single primary, and every byte must survive a reboot on a persistent disk.",[t(r),s(g,.6),h(y(r),l(r,1.6))]),e.step("Read replicas share the load and backups guard the data — but this box is precious: clone it carelessly and truth splits.",[x(.16,t(i),t(m),t(b)),x(.12,s(w,.5),s(v,.5),s(S,.5)),t(u),h(l(o,2),l(u,2)),f(1.2)],{hold:1})}}),re=Z({..._,scenes:[ee,te,ae,oe],outro:["So the ladder, top to bottom: a virtual machine gives you a whole computer and total control, at the price of assembling and babysitting it by hand. A Docker image trades that hand-built fragility for a reproducible artifact — the same bits everywhere — but still needs a host to run on. Kubernetes runs those images across a fleet and heals them automatically, at the price of real operational complexity. Each rung buys you something and charges you for it; you climb only as far as your problem actually needs.","And the fork matters as much as the ladder. Before you reach for any of it, ask what you're deploying. A stateless thing — a single-page app, static assets, anything that just answers with the same files — wants a CDN, where scaling is free and there's nothing to heal. A stateful thing — a database, a queue, anything that must remember — wants a managed, backed-up home built around a single source of truth. Most real systems are several of these at once: a static frontend on the edge, stateless services in containers, and a stateful database guarded like treasure. Good deployment is mostly just putting each piece in the home that fits it."]});export{re as default};
