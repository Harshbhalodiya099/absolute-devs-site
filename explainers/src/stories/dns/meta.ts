import { defineMeta } from "../../engine";

export const meta = defineMeta({
  slug: "dns",
  title: "How the internet finds google.com",
  intro: {
    eyebrow: "An interactive explainer",
    title: "Your browser has no idea where Google lives.",
    lead: "Every page you've ever visited began with the same quiet question: where is this, actually? Follow one DNS lookup through caches, root servers, and registries — pause it, scrub it, and break its assumptions on the way.",
    begin: "Follow the question",
  },
});
