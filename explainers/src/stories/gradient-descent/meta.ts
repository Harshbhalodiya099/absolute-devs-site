import { defineMeta } from "../../engine";

export const meta = defineMeta({
  slug: "gradient-descent",
  title: "How gradient descent makes machines learn",
  intro: {
    eyebrow: "An interactive explainer",
    title: "A neural network never has an insight. It rolls downhill.",
    lead: "Strip the word 'learning' of its mystique and one small loop remains: measure how wrong you are, find which way is downhill, take a tiny step, repeat a million times. Watch that loop invented, broken, and pushed off a cliff — every frame here comes from the real math, not choreography.",
    begin: "Start rolling",
  },
});
